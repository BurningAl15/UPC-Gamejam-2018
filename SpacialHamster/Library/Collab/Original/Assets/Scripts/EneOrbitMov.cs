using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EneOrbitMov : MonoBehaviour {
    private float radio = 50;
    private float angulo = 0;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //float pos_x ="POSXGATO"+ Mathf.Cos(angulo * Mathf.Deg2Rad) * radio;
        //float pos_y = "POSYGATO"-Mathf.Sin(angulo * Mathf.Deg2Rad) * radio;
        //transform.position = new Vector2(pos_x, pos_y);
        //angulo = angulo + 20 * Time.deltaTime;
    }
}
